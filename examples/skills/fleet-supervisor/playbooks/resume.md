---
id: resume
cooldown_min: 12
max_attempts: 3
ask: "{label} won't resume after {attempts} tries. Keep waiting?"
---
continue — get this across the finish line to merge. If you stopped on an error, timeout, or permission prompt, pick up where you stopped. Re-check CI on the exact head first.
